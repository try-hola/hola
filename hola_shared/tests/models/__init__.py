"""Package for model tests.

This package contains tests for the shared data models defined in the hola_shared.models package.
It follows a structured approach where each model has its own dedicated test file that validates:

1. Object creation with various parameter combinations
2. Type checking and validation behavior
3. Edge cases and error conditions
4. Compatibility with API usage patterns

These tests ensure that the shared models behave consistently across all components
that depend on them, both in the server and client applications.
"""
