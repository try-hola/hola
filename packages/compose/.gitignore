# Local environment override
.env

# Runtime data
logs/
data/

# Traefik ACME certificate store (contains private keys)
traefik/acme/

# Release version stamp — written into the bundle by CI (cli-release.yml), read
# by scripts/_common.sh to pin image tags on the host. Never committed.
VERSION
